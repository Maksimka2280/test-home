import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Penguin Dev - Profile',
  description: 'Penguin Dev - profile description',
};

export default function Profile() {
  return (
    <div>
      <h1>Profile</h1>
    </div>
  );
}
